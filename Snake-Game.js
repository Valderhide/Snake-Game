initialize();

function initialize(){
    var div = document.createElement('div');
    div.className = 'pixel';
    var gameboardDiv = document.getElementById("gameboard");
    gameboardDiv.appendChild(div);

    for(){//rows
        for(){//for each row, go through each dive

        }

    }

}